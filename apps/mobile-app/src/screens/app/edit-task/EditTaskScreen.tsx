import React from 'react';
import { View } from 'react-native';
import { IProps } from './EditTaskScreen.types';

const EditTaskScreen = ({ task, error, loading, updateTask }: IProps) => {
  return <View />;
};

export default React.memo(EditTaskScreen);
