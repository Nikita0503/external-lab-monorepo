import React from 'react';
import { View } from 'react-native';
import styles from './styles';

const TaskFileListSeparator = () => {
  return <View style={styles.container} />;
};

export default TaskFileListSeparator;
